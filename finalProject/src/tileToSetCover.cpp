/*
Author: Minsheng Zhang
		Shanshan Gao
*/

#include "tileToSetCover.hpp"
#include <vector>

using namespace std;

int
TileToSC::startConvert(vector<vector<string> > pieces,vector<string> board)
{
	int height, width;
	height = board.size();
	width = board[0].size();

	for(int i=0;i<pieces.size();i++)
	{
		//vector<vector<int> > tempSet = insertTile(pieces[i],board);
		// add the tempSet to the m_set

	}

	// check how many 

	return true;
}


